<div class="container">
  <h1 class="mt-5 mb-4">New movie</h1>

  <%= render 'shared/error', obj: @movie %>
  <%= render "form", movie: @movie %>

  <div class="mt-4">
    <%= link_to "Back to movies", movies_path, class: 'btn btn-secondary '%>
  </div>
</div>
